version: '3.7'

services:
  postgresql:
    image: postgres:12-alpine
    restart: always
    ports:
      - '5432:5432'
    volumes:
      - ./data:/var/lib/postgresql
    environment:
      - POSTGRES_DATABASE=db
      - POSTGRES_USER=user
      - POSTGRES_PASSWORD=qwerty123
